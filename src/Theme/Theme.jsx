import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0071E3",
    },
    white:{
      main:'white'
    }
  },
});

export default theme;